import React from 'react';

import { MyScheduleScreen } from '@/features/my-schedule';

/** Route only. Everything lives in `src/features/my-schedule/`. */
export default function MyScheduleRoute(): React.JSX.Element {
  return <MyScheduleScreen />;
}
