import React from 'react';

import { MapScreen } from '@/features/map';

/** Route only. Everything lives in `src/features/map/`. */
export default function MapRoute(): React.JSX.Element {
  return <MapScreen />;
}
