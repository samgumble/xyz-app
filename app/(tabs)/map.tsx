import React from 'react';

import { Screen } from '@/components';

/** Placeholder. Owned by `src/features/map/` — see BUILD-BRIEF.md. */
export default function MapScreen(): React.JSX.Element {
  return <Screen title="Map" subtitle="Placeholder screen. The map feature lands here." testID="screen-map" />;
}
